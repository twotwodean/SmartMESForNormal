# R4-B 매출·수금 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.
> **동시성 규칙:** 리뷰/검증 에이전트와 구현 에이전트를 동시에 돌리지 않는다. 리뷰/검증 에이전트는 `git checkout`/`stash`/파일 이동 금지.

**Goal:** FR-SAL-3 — 매출(청구서 발행)·수금(입금 등록) 관리: 출하/고객 기준 청구, 부분수금 반영, 미수금 현황.

**Architecture:** 신규 모델 `Invoice`·`Payment`(add-r4 마이그레이션). 도메인 `billing.ts`(invoiceStatusFor·outstanding) 순수함수. `billing-service.ts`가 청구 목록(수금합·미수금·상태)·청구 생성·수금 등록($transaction). API 조회(requireUser)+변경(requireRole OPERATOR)+audit. 화면 `/mockups/billing`. 네비 영업관리 > 매출/수금.

**Tech Stack:** Prisma+SQLite(String enum 유지), 기존 스택.

---

## File Structure
| 파일 | 책임 |
|---|---|
| `prisma/schema.prisma` | (수정) Invoice·Payment 추가 + Supplier/Shipment 역참조 |
| `prisma/migrations/**` | add-r4 마이그레이션 |
| `prisma/seed.ts` | (수정) 청구 1·수금 1 샘플 |
| `lib/domain/billing.ts` (+test) | invoiceStatusFor·outstanding |
| `lib/domain/types.ts` | (수정) InvoiceStatus |
| `lib/services/billing-service.ts` (+test) | 청구 목록/생성·수금 등록 |
| `app/api/invoices/route.ts` | GET/POST |
| `app/api/payments/route.ts` | POST(OPERATOR)+audit |
| `app/mockups/billing/page.tsx`+client | 매출/수금 화면 |
| `app/mockups/layout.tsx` | (수정) 네비 매출/수금 |

---

### Task 1: 스키마 + 도메인 + 타입

- [ ] **Step 1: `prisma/schema.prisma`** — 모델 추가 + 역참조.
`Supplier` 모델에 `invoices Invoice[]` 추가. `Shipment` 모델에 `invoices Invoice[]` 추가.
```prisma
// status: ISSUED | PARTIAL | PAID
model Invoice {
  id         String    @id @default(cuid())
  code       String    @unique
  customerId String
  shipmentId String?
  amount     Int
  status     String    @default("ISSUED")
  issuedAt   DateTime  @default(now())
  customer   Supplier  @relation(fields: [customerId], references: [id])
  shipment   Shipment? @relation(fields: [shipmentId], references: [id])
  payments   Payment[]
}

model Payment {
  id        String   @id @default(cuid())
  invoiceId String
  amount    Int
  paidAt    DateTime @default(now())
  invoice   Invoice  @relation(fields: [invoiceId], references: [id])
}
```

- [ ] **Step 2: 마이그레이션** — `npx prisma format` → `npx prisma validate` → `npx prisma migrate dev --name add-r4` → `npx prisma generate`. (현 seed로 마이그레이션 통과 확인.)

- [ ] **Step 3: `lib/domain/types.ts`에 추가**
```ts
export type InvoiceStatus = "ISSUED" | "PARTIAL" | "PAID";
```

- [ ] **Step 4: 실패 테스트 `lib/domain/billing.test.ts`**
```ts
import { describe, it, expect } from "vitest";
import { invoiceStatusFor, outstanding } from "@/lib/domain/billing";

describe("invoiceStatusFor", () => {
  it("수금액에 따라 상태", () => {
    expect(invoiceStatusFor(1000, 0)).toBe("ISSUED");
    expect(invoiceStatusFor(1000, 400)).toBe("PARTIAL");
    expect(invoiceStatusFor(1000, 1000)).toBe("PAID");
    expect(invoiceStatusFor(1000, 1200)).toBe("PAID");
  });
});

describe("outstanding", () => {
  it("미수금 = 청구액 - 수금액, 음수 방지", () => {
    expect(outstanding(1000, 300)).toBe(700);
    expect(outstanding(1000, 1000)).toBe(0);
    expect(outstanding(1000, 1500)).toBe(0);
  });
});
```
Run → FAIL. `lib/domain/billing.ts`:
```ts
import type { InvoiceStatus } from "@/lib/domain/types";

/** 미수금 = max(0, 청구액 - 수금액) */
export function outstanding(amount: number, paid: number): number {
  return Math.max(0, amount - paid);
}

/** 수금액 기준 청구 상태 */
export function invoiceStatusFor(amount: number, paid: number): InvoiceStatus {
  if (paid <= 0) return "ISSUED";
  if (paid >= amount) return "PAID";
  return "PARTIAL";
}
```
Run → PASS (2).

- [ ] **Step 5: Commit**
```bash
git add prisma/schema.prisma prisma/migrations lib/domain/billing.ts lib/domain/billing.test.ts lib/domain/types.ts
git commit -m "feat(r4): 매출/수금 스키마(Invoice·Payment)+마이그레이션 + billing 도메인 + 테스트"
```

---

### Task 2: seed + billing-service + 테스트

- [ ] **Step 1: `prisma/seed.ts`** — 정리 블록에 `payment, invoice` deleteMany를 (invoice→payment FK 순서상 payment 먼저 삭제, invoice 나중; 실제로는 payment가 invoice 참조 → payment 먼저) 기존 정리보다 위/적절 위치에 추가. 생성부 끝에: 기존 customer(CUS-001 한빛기계)·shipment(SH-2607-001) 변수 참조하여
  - Invoice 1: `INV-2607-001`, customerId=한빛기계, shipmentId=SH-2607-001, amount=1_200_000, status "PARTIAL".
  - Payment 1: invoiceId=위 인보이스, amount=500_000.
  (seed는 status를 "PARTIAL"로 직접 넣되, 서비스 조회 시 payments 합으로 재계산되므로 표시 일관. 콘솔 요약에 청구/수금 추가.)

- [ ] **Step 2: 재시드 확인** — `npm run db:seed` 성공(청구 1·수금 1 로그).

- [ ] **Step 3: `lib/services/billing-service.ts`**
```ts
import { prisma } from "@/lib/db";
import { invoiceStatusFor, outstanding } from "@/lib/domain/billing";
import type { InvoiceStatus } from "@/lib/domain/types";

export interface InvoiceRow {
  id: string;
  code: string;
  customerName: string;
  shipmentCode: string | null;
  amount: number;
  paid: number;
  outstanding: number;
  status: InvoiceStatus;
  issuedAt: string;
}

export async function listInvoices(): Promise<InvoiceRow[]> {
  const rows = await prisma.invoice.findMany({ include: { customer: true, shipment: true, payments: true }, orderBy: { issuedAt: "desc" } });
  return rows.map((r) => {
    const paid = r.payments.reduce((a, p) => a + p.amount, 0);
    return {
      id: r.id, code: r.code, customerName: r.customer.name,
      shipmentCode: r.shipment?.code ?? null,
      amount: r.amount, paid, outstanding: outstanding(r.amount, paid),
      status: invoiceStatusFor(r.amount, paid), issuedAt: r.issuedAt.toISOString(),
    };
  });
}

let invSeq = 0;
export async function createInvoice(input: { customerId: string; amount: number; shipmentId?: string }) {
  if (input.amount <= 0) throw new Error("청구액은 1 이상이어야 합니다.");
  invSeq += 1;
  const code = `INV-${Date.now().toString().slice(-6)}-${String(invSeq).padStart(3, "0")}`;
  return prisma.invoice.create({ data: { code, customerId: input.customerId, amount: input.amount, shipmentId: input.shipmentId ?? null, status: "ISSUED" } });
}

/** 수금 등록(원자적): Payment 생성 + 청구 상태 재계산 */
export async function recordPayment(input: { invoiceId: string; amount: number }) {
  if (input.amount <= 0) throw new Error("수금액은 1 이상이어야 합니다.");
  return prisma.$transaction(async (tx) => {
    const inv = await tx.invoice.findUnique({ where: { id: input.invoiceId }, include: { payments: true } });
    if (!inv) throw new Error("청구를 찾을 수 없습니다.");
    const payment = await tx.payment.create({ data: { invoiceId: inv.id, amount: input.amount } });
    const paid = inv.payments.reduce((a, p) => a + p.amount, 0) + input.amount;
    const updated = await tx.invoice.update({ where: { id: inv.id }, data: { status: invoiceStatusFor(inv.amount, paid) } });
    return { payment, invoice: updated };
  });
}
```

- [ ] **Step 4: 테스트 `lib/services/billing-service.test.ts`** (afterAll 재seed)
```ts
import { describe, it, expect, afterAll } from "vitest";
import { execSync } from "node:child_process";
import { prisma } from "@/lib/db";
import { listInvoices, recordPayment } from "@/lib/services/billing-service";

afterAll(() => { execSync("npm run db:seed", { stdio: "ignore" }); });

describe("billing-service", () => {
  it("seed 청구가 수금합·미수금과 함께 조회된다(INV-2607-001 = 1,200,000 / 500,000 수금 → 700,000 미수, PARTIAL)", async () => {
    const rows = await listInvoices();
    const inv = rows.find((r) => r.code === "INV-2607-001")!;
    expect(inv.paid).toBe(500_000);
    expect(inv.outstanding).toBe(700_000);
    expect(inv.status).toBe("PARTIAL");
  });
  it("수금 등록 시 상태가 PAID로 갱신된다", async () => {
    const inv = await prisma.invoice.findFirstOrThrow({ where: { code: "INV-2607-001" } });
    const { invoice } = await recordPayment({ invoiceId: inv.id, amount: 700_000 }); // 500k+700k=1.2M → PAID
    expect(invoice.status).toBe("PAID");
    const row = (await listInvoices()).find((r) => r.code === "INV-2607-001")!;
    expect(row.outstanding).toBe(0);
  });
});
```
Run → FAIL(모듈) → 구현 후 PASS (2). afterAll 재seed 원복.

- [ ] **Step 5: 확인 + Commit** — `npm run db:seed` → `npm test -- billing` PASS → `npx tsc --noEmit` 클린.
```bash
git add prisma/seed.ts lib/services/billing-service.ts lib/services/billing-service.test.ts
git commit -m "feat(r4): 매출/수금 서비스(청구 목록/생성·수금 등록 트랜잭션) + seed + 테스트"
```

---

### Task 3: API + 화면 + 네비

- [ ] **Step 1: `app/api/invoices/route.ts`** — `export const runtime="nodejs"`. GET listInvoices(requireUser). POST createInvoice(requireRole OPERATOR, body {customerId, amount, shipmentId?}, try/catch 400, audit("CREATE","Invoice",inv.id), 201).
- [ ] **Step 2: `app/api/payments/route.ts`** — POST requireRole OPERATOR, body {invoiceId, amount}, recordPayment + audit("PAYMENT","Payment",result.payment.id), try/catch 400, 201.
guard 패턴·runtime nodejs·`{error}` shape 기존 라우트(sales/procurement) 일치.
- [ ] **Step 3: `app/mockups/billing/page.tsx`** (server, force-dynamic) — Promise.all([listInvoices(), listSuppliers("CUSTOMER") from procurement-service, listShipments() from sales-service]) → client.
- [ ] **Step 4: `app/mockups/billing/billing-client.tsx`** (client, ToastProvider, sales-client.tsx 미러):
  - SectionHeader "영업 · 매출/수금" description "청구 발행 · 수금 등록 · 미수금", actions "청구 발행" Button(Dialog).
  - KPI(선택): 총 청구액·총 수금액·총 미수금 합계(KPITile 있으면).
  - DataTable(invoices), enableFilter: 청구번호(mono)·고객(customerName)·출하(shipmentCode??"-")·청구액(amount, 천단위 콤마 toLocaleString)·수금액(paid, 콤마)·미수금(outstanding, 콤마·강조)·상태(StatusPill: ISSUED warn "미수금"/PARTIAL primary "부분수금"/PAID ok "완납")·발행일(issuedAt.slice0,10)·액션(status!=="PAID"→ "수금" Button → 수금 Dialog).
  - 청구 발행 Dialog: 고객 Select(customers)·출하 Select(shipments, 미지정 허용 sentinel "__NONE__"→ omit)·청구액 Input(number) → POST /api/invoices → toast "청구 발행됨"+refresh; 403 "권한 없음".
  - 수금 Dialog(선택 invoice 보관): 수금액 Input(number, default = 해당 invoice.outstanding) → POST /api/payments {invoiceId, amount} → toast "수금 등록됨"+refresh; 403.
  D1 컴포넌트/토큰만·no any. 금액은 정수(원), toLocaleString 표시. 상태→tone 로컬 helper. Radix Select 빈값 금지 → sentinel 사용.
- [ ] **Step 5: `app/mockups/layout.tsx` 네비** — "영업관리" 그룹 items에 `{ label:"매출/수금", href:"/mockups/billing", icon: Wallet }` 추가(수주/출하 뒤). CRUMB `"/mockups/billing":[{label:"영업관리"},{label:"매출/수금"}]`. lucide `Wallet` import(중복 확인).
- [ ] **Step 6: 검증 + Commit** — `npx tsc --noEmit`, `npm run build`(/mockups/billing dynamic, /api/invoices, /api/payments).
```bash
git add app/api/invoices app/api/payments app/mockups/billing app/mockups/layout.tsx
git commit -m "feat(r4): 매출/수금 API + 화면 + 네비"
```

---

### Task 4: 전체 검증 + 실렌더
- [ ] **Step 1** `npm run db:seed` → `npm test`(billing 4 포함 통과, 카운트) → `npx tsc --noEmit` → `npm run build`.
- [ ] **Step 2 실렌더(Playwright)** `npm run dev`(3001). admin → `/mockups/billing`:
  - seed 청구 INV-2607-001 표시: 청구액 1,200,000 / 수금 500,000 / 미수금 700,000 / 부분수금(PARTIAL).
  - "수금" → 700,000 입력 → 등록 → toast "수금 등록됨" → 미수금 0 / 완납(PAID).
  - "청구 발행" → 고객·(출하 미지정)·금액 → 발행 → 목록 증가, 미수금 상태.
  - viewer → 청구 발행/수금 각 403 "권한 없음".
  Report ACTUAL. 종료 후 `npm run db:seed`, `npx kill-port 3001`, 스크래치 삭제.
- [ ] **Step 3** 이슈 수정 시 별도 커밋.

---

## Self-Review 결과
**Spec 커버리지:** FR-SAL-3(매출·수금) → Invoice/Payment + createInvoice·recordPayment(트랜잭션·상태 재계산) + 미수금 현황 ✅. RBAC(변경 OPERATOR+)+audit ✅.
**플레이스홀더 스캔:** 없음.
**타입 일관성:** InvoiceStatus(domain)·InvoiceRow(service). invoiceStatusFor/outstanding 도메인 재사용. 금액 Int(원).
**주의:** 쓰기 테스트 afterAll 재seed. recordPayment $transaction으로 Payment+상태 정합. Radix Select 빈값 금지→sentinel. 금액 콤마 표시(toLocaleString). shipmentId·shipment 역참조 양방향.
**범위:** R4-B(매출/수금). R4-A(MRP)와 함께 R4 완료 → 나머지(금형·FMEA/ISIR·APS·무역)는 별도 프로젝트/후속.
