# R4-A MRP(소요량 산출) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.
> **동시성 규칙:** 리뷰/검증 에이전트와 구현 에이전트를 동시에 돌리지 않는다. 리뷰/검증 에이전트는 `git checkout`/`stash`/파일 이동 금지.

**Goal:** FR-MAT-3 — 소요량 계산(MRP): 수요(수주+BOM전개) vs 공급(현재고+입고예정) + 안전재고 반영 → 품목별 순소요량 + 구매/생산 제안.

**Architecture:** 순수함수 `lib/domain/mrp.ts`(netRequirement·grossDemand, explodeBom 재사용). `mrp-service.ts`가 SalesOrder(수요)·BomComponent(전개)·listStock(현재고)·PurchaseOrder 미입고(입고예정)·Item.safetyStock 조합 → MrpRow[]. 읽기 전용 API/화면. DB 변경 없음(기존 스키마만 사용).

**Tech Stack:** 기존 스택. 신규 모델/마이그레이션 없음.

---

## File Structure
| 파일 | 책임 |
|---|---|
| `lib/domain/mrp.ts` (+test) | netRequirement, grossDemand(BOM 전개 집계) |
| `lib/services/mrp-service.ts` (+test) | computeMrp() 조합 |
| `app/api/mrp/route.ts` | GET computeMrp (requireUser) |
| `app/mockups/mrp/page.tsx`+client | 소요량 화면 |
| `app/mockups/layout.tsx` | (수정) 네비 자재관리 > 소요량(MRP) |

---

### Task 1: MRP 도메인 + 테스트

- [ ] **Step 1: 실패 테스트 `lib/domain/mrp.test.ts`**
```ts
import { describe, it, expect } from "vitest";
import { netRequirement, grossDemand } from "@/lib/domain/mrp";
import type { BomLink } from "@/lib/domain/bom";

describe("netRequirement", () => {
  it("순소요 = 총소요 + 안전재고 - 현재고 - 입고예정", () => {
    expect(netRequirement(100, 30, 20, 10)).toBe(80); // 100+20-30-10
  });
  it("공급이 충분하면 0으로 캡", () => {
    expect(netRequirement(50, 60, 0, 20)).toBe(0);
  });
});

describe("grossDemand", () => {
  const bom: BomLink[] = [
    { parentId: "FIN", childId: "SUB", qtyPer: 2 },
    { parentId: "SUB", childId: "RAW", qtyPer: 3 },
  ];
  it("완제품 수요를 BOM 다단 전개해 품목별 총소요 집계(완제품 자체 포함)", () => {
    const g = grossDemand([{ itemId: "FIN", qty: 10 }], bom);
    expect(g.get("FIN")).toBe(10);
    expect(g.get("SUB")).toBe(20); // 10*2
    expect(g.get("RAW")).toBe(60); // 20*3
  });
  it("동일 품목 다중 수요는 합산", () => {
    const g = grossDemand([{ itemId: "FIN", qty: 5 }, { itemId: "SUB", qty: 4 }], bom);
    expect(g.get("SUB")).toBe(14); // 5*2 + 4
  });
});
```
Run → FAIL(모듈 없음).

- [ ] **Step 2: `lib/domain/mrp.ts`**
```ts
import { explodeBom, type BomLink } from "@/lib/domain/bom";

/** 순소요량 = max(0, 총소요 + 안전재고 - 현재고 - 입고예정) */
export function netRequirement(gross: number, onHand: number, safety: number, incoming: number): number {
  return Math.max(0, gross + safety - onHand - incoming);
}

/** 수요(주문)들을 BOM 전개해 품목별 총소요량 집계(완제품 자체 + 하위 부품, 다단) */
export function grossDemand(orders: { itemId: string; qty: number }[], bom: BomLink[]): Map<string, number> {
  const g = new Map<string, number>();
  const add = (id: string, q: number) => g.set(id, (g.get(id) ?? 0) + q);
  for (const o of orders) {
    add(o.itemId, o.qty);
    for (const [child, q] of explodeBom(o.itemId, o.qty, bom)) add(child, q);
  }
  return g;
}
```
Run → PASS (4).

- [ ] **Step 3: Commit**
```bash
git add lib/domain/mrp.ts lib/domain/mrp.test.ts
git commit -m "feat(r4): MRP 도메인(netRequirement·grossDemand BOM전개) + 테스트"
```

---

### Task 2: mrp-service + 테스트

- [ ] **Step 1: `lib/services/mrp-service.ts`**
```ts
import { prisma } from "@/lib/db";
import { grossDemand, netRequirement } from "@/lib/domain/mrp";
import type { BomLink } from "@/lib/domain/bom";
import { listStock } from "@/lib/services/inventory-service";

export type MrpSuggestion = "PURCHASE" | "PRODUCE" | "NONE";

export interface MrpRow {
  itemId: string;
  code: string;
  name: string;
  uom: string;
  gross: number;
  onHand: number;
  safety: number;
  incoming: number;
  net: number;
  suggestion: MrpSuggestion;
}

/** 수주(미완료) + BOM전개 = 수요, 현재고 + 미입고 PO = 공급, 안전재고 반영 → 품목별 순소요 */
export async function computeMrp(): Promise<MrpRow[]> {
  const [items, bomRows, openSo, openPo, stock] = await Promise.all([
    prisma.item.findMany({ orderBy: { code: "asc" } }),
    prisma.bomComponent.findMany(),
    prisma.salesOrder.findMany({ where: { status: { in: ["ORDERED", "PRODUCING"] } }, select: { itemId: true, qty: true } }),
    prisma.purchaseOrder.findMany({ where: { status: { in: ["ORDERED", "PARTIAL"] } }, include: { receipts: true } }),
    listStock(),
  ]);

  const bom: BomLink[] = bomRows.map((b) => ({ parentId: b.parentId, childId: b.childId, qtyPer: b.qtyPer }));
  const gross = grossDemand(openSo, bom);
  const onHandMap = new Map(stock.map((s) => [s.itemId, s.qty]));
  const incomingMap = new Map<string, number>();
  for (const po of openPo) {
    const received = po.receipts.reduce((a, r) => a + r.qty, 0);
    incomingMap.set(po.itemId, (incomingMap.get(po.itemId) ?? 0) + Math.max(0, po.qty - received));
  }
  const madeIds = new Set(bomRows.map((b) => b.parentId)); // BOM 부모 = 사내 생산 품목

  return items.map((it) => {
    const g = gross.get(it.id) ?? 0;
    const onHand = onHandMap.get(it.id) ?? 0;
    const incoming = incomingMap.get(it.id) ?? 0;
    const net = netRequirement(g, onHand, it.safetyStock, incoming);
    const suggestion: MrpSuggestion = net <= 0 ? "NONE" : madeIds.has(it.id) ? "PRODUCE" : "PURCHASE";
    return { itemId: it.id, code: it.code, name: it.name, uom: it.uom, gross: g, onHand, safety: it.safetyStock, incoming, net, suggestion };
  });
}
```

- [ ] **Step 2: 테스트 `lib/services/mrp-service.test.ts`** (읽기 전용 — 재seed 불필요)
```ts
import { describe, it, expect } from "vitest";
import { computeMrp } from "@/lib/services/mrp-service";
import { netRequirement } from "@/lib/domain/mrp";

describe("mrp-service", () => {
  it("모든 품목에 대해 행을 반환하고 순소요 공식이 일관된다", async () => {
    const rows = await computeMrp();
    expect(rows.length).toBeGreaterThan(0);
    for (const r of rows) {
      expect(r.net).toBe(netRequirement(r.gross, r.onHand, r.safety, r.incoming));
      expect(r.net).toBeGreaterThanOrEqual(0);
    }
  });
  it("순소요가 있으면 제안이 PURCHASE 또는 PRODUCE, 없으면 NONE", async () => {
    const rows = await computeMrp();
    for (const r of rows) {
      if (r.net > 0) expect(["PURCHASE", "PRODUCE"]).toContain(r.suggestion);
      else expect(r.suggestion).toBe("NONE");
    }
  });
});
```
Run → FAIL(모듈) → 구현 후 PASS (2). 읽기 전용이라 DB 변경 없음.

- [ ] **Step 3: 확인 + Commit** — `npm run db:seed` → `npm test -- mrp` PASS → `npx tsc --noEmit` 클린.
```bash
git add lib/services/mrp-service.ts lib/services/mrp-service.test.ts
git commit -m "feat(r4): MRP 서비스(수주·BOM·재고·미입고PO 조합 순소요) + 테스트"
```

---

### Task 3: API + 화면 + 네비

- [ ] **Step 1: `app/api/mrp/route.ts`**
```ts
import { NextResponse } from "next/server";
import { requireUser } from "@/lib/api/guard";
import { computeMrp } from "@/lib/services/mrp-service";

export const runtime = "nodejs";

export async function GET() {
  const g = await requireUser();
  if ("res" in g) return g.res;
  const rows = await computeMrp();
  return NextResponse.json(rows);
}
```
(requireUser 호출 형태는 기존 라우트와 일치시킬 것 — 인자 없이 `requireUser()`. `app/api/purchase-orders/route.ts` 확인.)

- [ ] **Step 2: `app/mockups/mrp/page.tsx`** (server, force-dynamic)
```tsx
import { computeMrp } from "@/lib/services/mrp-service";
import { MrpClient } from "./mrp-client";

export const dynamic = "force-dynamic";

export default async function MrpPage() {
  const rows = await computeMrp();
  return <MrpClient rows={rows} />;
}
```

- [ ] **Step 3: `app/mockups/mrp/mrp-client.tsx`** (client)
  - 読기 전용 화면(변경 없음 → ToastProvider 불필요, useRouter.refresh만 "새로고침" 버튼에 사용).
  - SectionHeader "자재 · 소요량 산출(MRP)" description "수주·BOM 전개 대비 현재고·입고예정 → 순소요", actions = "새로고침" Button(onClick router.refresh()).
  - KPI 요약(선택): 순소요 발생 품목수 / 구매 제안 / 생산 제안 — KPITile 있으면 사용, 없으면 생략.
  - DataTable(rows), enableFilter: 품목코드(code, mono)·품목명(name)·총소요(gross)·현재고(onHand)·안전재고(safety)·입고예정(incoming)·**순소요(net, 강조: net>0이면 굵게/색상)**·제안(StatusPill: PURCHASE warn "구매", PRODUCE primary "생산", NONE neutral "-").
  - `MrpRow` 타입은 `@/lib/services/mrp-service`에서 import. no any. D1 컴포넌트/토큰만. 로컬 suggestion→{label,tone} helper.
- [ ] **Step 4: `app/mockups/layout.tsx` 네비** — "자재관리" 그룹 items에 `{ label:"소요량(MRP)", href:"/mockups/mrp", icon: Calculator }` 추가(구매/발주 뒤). CRUMB `"/mockups/mrp":[{label:"자재관리"},{label:"소요량(MRP)"}]`. lucide `Calculator` import(중복 확인). (자재관리 그룹이 없다면 재고관리 그룹에 배치.)
- [ ] **Step 5: 검증 + Commit** — `npx tsc --noEmit`, `npm run build`(/mockups/mrp dynamic, /api/mrp).
```bash
git add app/api/mrp app/mockups/mrp app/mockups/layout.tsx
git commit -m "feat(r4): MRP API + 소요량 화면 + 네비"
```

---

### Task 4: 전체 검증 + 실렌더
- [ ] **Step 1** `npm run db:seed` → `npm test`(mrp 6 포함 통과, 카운트) → `npx tsc --noEmit` → `npm run build`.
- [ ] **Step 2 실렌더(Playwright)** `npm run dev`(3001). admin → `/mockups/mrp`:
  - 표 렌더, 콘솔 에러 없음. 품목별 순소요·제안 표시. seed의 수주(SO-2607-001 완제품 200)·BOM 전개로 하위 품목에 총소요가 잡히는지, 현재고/안전재고 대비 순소요 계산이 맞는지 1개 품목 수기 검산.
  - 제안 컬럼: 사내생산 품목=생산, 구매 품목=구매 표시 확인.
  - "새로고침" 동작.
  - viewer 로그인 → /mockups/mrp 조회 가능(읽기 전용, 403 없음).
  Report ACTUAL. 종료 후 `npm run db:seed`, `npx kill-port 3001`, 스크래치 삭제.
- [ ] **Step 3** 이슈 수정 시 별도 커밋.

---

## Self-Review 결과
**Spec 커버리지:** FR-MAT-3(MRP: 계획·현재고·안전재고 반영) → grossDemand(수주+BOM)·netRequirement(안전재고·입고예정 반영)·computeMrp ✅. 읽기 전용이라 RBAC은 requireUser만.
**플레이스홀더 스캔:** 없음.
**타입 일관성:** BomLink(domain) 재사용, MrpRow/MrpSuggestion(service). explodeBom 재사용.
**주의:** 읽기 전용 → 마이그레이션/재seed 불필요, 쓰기 테스트 아님. 미입고량 = qty - Σreceipts(음수 방지). madeIds=BOM 부모.
**범위:** R4-A(MRP). 이후 R4-B(매출/수금).
